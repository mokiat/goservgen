package grpcdsl

func FieldIndex(index int) {
	// TODO: From the render context get the current Node and attach
	// a grpcField type, if one is not available yet, and then set
	// the FieldIndex value of the grpcField.
}
